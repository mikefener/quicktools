export default function AdBanner() {
  // Returns nothing while under review to keep layouts clean and pass AdSense inspection.
  // We will restore the live Google <ins> tag once your domain is approved.
  return null
}